import { dependencies as cart } from './Cart/default'
import { dependencies as checkout } from './Checkout/default'
import { dependencies as homepage } from './Homepage/default'
import { dependencies as productDetail } from './ProductDetail/default'

export default {
  cart,
  checkout,
  homepage,
  productDetail
}
