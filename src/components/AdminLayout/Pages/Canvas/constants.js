import { element } from '../../../Cart/default'
import { element as checkoutelement } from '../../../Checkout/default'
import { element as homepageelement } from '../../../Homepage/default'
import { element as productDetailelement } from '../../../ProductDetail/default'

export const DEFAULT_COLUMN = {
  maxH: 1,
  w: 1,
  h: 1
}

export const TEST_DATA = element

export const CHECKOUT_TEST_DATA = checkoutelement
export const HOMEPAGE_TEST_DATA = homepageelement
export const PRODUCT_DETAIL_TEST_DATA = productDetailelement

export const ROW_HEIGHT = 100
